let SmartFormat : {} = {
    getDate: function(Language : string, NewDate : Date){
        let currentDate : Date = new Date

        function _SPLIT_DATE(date : Date){
            let year = date.getFullYear()
            let month = date.getMonth()+1
            let day = date.getDate()
            return {year,month,day}
        }

        

    }
}